#include "progressive/fatal.hpp"
#include <string>
#include <nlohmann/json.hpp>
namespace progressive {
using json = nlohmann::json;
bool fatal_validate(const std::string& i) { return !i.empty(); }
std::string fatal_process(const std::string& i) { return i; }
json fatal_toJson(const std::string& i) { return json::object(); }
}
