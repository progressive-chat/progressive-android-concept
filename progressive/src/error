#include "progressive/error.hpp"
#include <string>
#include <nlohmann/json.hpp>
namespace progressive {
using json = nlohmann::json;
bool error_validate(const std::string& i) { return !i.empty(); }
std::string error_process(const std::string& i) { return i; }
json error_toJson(const std::string& i) { return json::object(); }
}
